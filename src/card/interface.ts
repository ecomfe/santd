export interface State {
    bordered: boolean,
    type?: string,
    title?: string,
    size?: string,
    tabList?: any[],
    loading?: boolean,
    hoverable?: boolean,
    defaultActiveKey?: string,
    activeTabKey?: string,
    actions?: any[],
}
