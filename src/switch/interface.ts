interface SizeMap {
    [tag: string]: string;
}
export interface State {
    sizeMap: SizeMap,
    disabled: boolean,
    defaultChecked: boolean,
    checked: boolean,
    loading: boolean
}

export interface Props {
}

export interface Computed {

}