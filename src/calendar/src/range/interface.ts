export interface RangePanelState {
    visible: boolean;
    prefixCls: string;
    showToday: boolean;
    showDateInput: boolean;
    focusablePanel: boolean;
}

export interface RangePanelProps {
    direction: string;
}

export interface RangePanelComputed {
}